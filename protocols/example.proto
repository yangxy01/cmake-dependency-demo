syntax = "proto3";

package example;

import "common/common.proto";  // 导入 common.proto

message HelloRequest {
    string message = 1;
    common.Person person = 2;
}

message HelloResponse {
    string message = 1;
}